# namespace = cultistplot

# ##Convert to Nurgle Plot Chain##
# character_event = {
    # id = cultistplot.1
    # desc = EVTDESC_CULTISTPLOT_1 #"With the help of other acolytes and children of Plaguefather, you have succeeded in placing several artefacts of Nurgle in the chamber's of [ROOT.plot_target_char.GetTitledName]. [ROOT.plot_target_char.GetSheHeCap] should be feeling the Plaguelord's love even now. It is inevitable that [ROOT.plot_target_char.GetTitledFirstName] will join us soon..."
    # picture = GFX_evt_whispers
    # border = GFX_event_normal_frame_intrigue
    
    # capable_only = yes
    # prisoner = no
    
    # trigger = {
    # has_plot = plot_convert_nurgle
	# plot_target_char = {
            # prisoner = no
            # NOT = { trait = incapable }
        # }
	# plot_power_contribution = 
        # {
            # plotter = ROOT
            # power = 0.025
        # }
        # }
		
    # mean_time_to_happen = {
        # months = 360
        
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.10
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.20
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.30
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.40
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.50
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.60
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.70
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.80
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.90
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.5
            # }
        # }
        
        # modifier = {
            # factor = 0.33
                # plot_power = 1.0
		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 1.5
		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 2.0
		# }
    # }
	
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_1A #"I'll bide my time..."
        # trigger = {
            # plot_power = 0.20
			# NOT = { plot_power = 0.21 }
        # }
		# custom_tooltip = { text = EVTTOOLTIP_CULTISTPLOT_1A } #"it seems [ROOT.plot_target_char.GetTitledFirstName] is not quite ready for the gifts of Grandfather Nurgle."
        # plot_target_char = { character_event = { id = cultistplot.2 days = 7 } }
    # }
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_1B #"Let us give [ROOT.plot_target_char.GetHerHim] one of Nurgle's gifts."
        # trigger = {
            # plot_power = 0.50
			# plot_target_char = {
				# NOT = {
					# OR = {
						# trait = ill
						# trait = syphilitic
						# trait = leper
						# trait = has_creeping_buboes
						# trait = has_measles
						# trait = has_typhus
						# trait = pneumonic
						# trait = has_tomb_rot
						# trait = has_bone_auge
						# trait = has_nurgles_rot
					# }
				# }
			# }
        # }
		# custom_tooltip = { text = EVTTOOLTIP_CULTISTPLOT_1B } #"This will help [ROOT.plot_target_char.GetTitledFirstName] see the joy one can receive from the Lord of Decay.
        # plot_target_char = { character_event = { id = cultistplot.2 days = 7 } }
    # }
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_1C #"The time has come to show [ROOT.plot_target_char.GetTitledFirstName] the glory of Father Nurgle."
        # trigger = {
            # OR = {
				# plot_power = 1.0
				# plot_target_char = {
					# OR = {
						# trait = ill
						# trait = syphilitic
						# trait = leper
						# trait = has_creeping_buboes
						# trait = has_measles
						# trait = has_typhus
						# trait = pneumonic
						# trait = has_tomb_rot
						# trait = has_bone_auge
						# trait = has_nurgles_rot
					# }
				# }
			# }
        # }
        # plot_target_char = { character_event = { id = cultistplot.3 days = 7 } }
    # }
	
	# }
	
# character_event = {
# id = cultistplot.2
    # desc = EVTDESC_CULTISTPLOT_2 #"Over the past few days you have been hearing voices and having strange dreams of plague victims cavorting in piles of filth, even as their flesh rots away. At first you questioned your own sanity, but this morning you awoke to find you had contracted a horrible disease!" 
    # picture = "GFX_evt_nurgle"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_2A #"[ROOT.GetRandomGodNameCap], no!"
		# health = 5

		
			# if = {
				# limit = {
					# NOT = {
						# has_dlc = "Reapers"
					# }
				# }
						# random_list = {
							# 50 = { 
								# ROOT = { 
									# add_trait = ill
								# }
							# }
							# 15 = { 
								# ROOT = { 
									# add_trait = pneumonic
								# }
							# }
							# 5 = { 
								# ROOT = { 
									# add_trait = syphilitic
								# }
							# }
							# 5 = { 
								# ROOT = { 
									# add_trait = leper
								# }
							# }
							# 5 = { 
								# ROOT = { 
									# add_trait = has_creeping_buboes
								# }
							# }
							# 5 = { 
								# ROOT = { 
									# add_trait = has_measles
								# }
							# }
							# 5 = { 
								# ROOT = { 
									# add_trait = has_typhus
								# }
							# }
							# 5 = { 
								# ROOT = { 
									# add_trait = has_tomb_rot
								# }
							# }
							# 4 = { 
								# ROOT = { 
									# add_trait = has_bone_auge
								# }
							# }
							# 1 = { 
								# ROOT = { 
									# add_trait = has_nurgles_rot
								# }
							# }
						# }
			# }
			# if = {
				# limit = {
					# has_dlc = "Reapers"
				# }
				# add_symptom_effect = yes
			# }
		
	# }
	
	# }
	
# character_event = {
# id = cultistplot.3
    # desc = EVTDESC_CULTISTPLOT_3 #"Over the past few days you have been hearing voices and having strange dreams of plague victims cavorting in piles of filth, even as their flesh rots away. However, today you received an anonymous message: 'We can help you. Follow these directions and come alone...'" 
    # picture = "GFX_evt_council"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_3A #"No, never!"
	# ai_chance = {
      # factor = 50
		# modifier = {
			# factor = 2.0
			# trait = brave
		# }
		# modifier = {
			# factor = 4.0
			# trait = zealous
		# }
		# modifier = {
			# factor = 2.0
			# is_strong_trigger = yes
		# }
		# modifier = {
			# factor = 2.0
			# trait = just
		# }
      # modifier = {
			# factor = 2.0
			# trait = diligent
		# }
	# }
	# custom_tooltip = { text = EVTTOOLTIP_CULTISTPLOT_3A } #" As awful as this is, nothing good could happen by going to the sewers alone at night."
	# add_trait = stressed
	# add_trait = depressed
	# FROM = { character_event = { id = cultistplot.4 days = 7 } }
	# }
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_3B #"Fine! Anything to stop these nightmares."
	# ai_chance = {
		# factor = 50
		# modifier = {
			# factor = 2.0
			# trait = content
		# }
		# modifier = {
			# factor = 2.0
			# trait = envious
		# }
		# modifier = {
			# factor = 2.0
			# trait = slothful
		# }
		# modifier = {
			# factor = 2.0
			# trait = greedy
		# }
		# modifier = {
			# factor = 2.0
			# trait = gluttonous
		# }
		# modifier = {
			# factor = 2.0
			# trait = kind
		# }
		# modifier = {
			# factor = 2.0
			# trait = ill
		# }
		# modifier = {
			# factor = 2.0
			# trait = weak
		# }
		# modifier = {
			# factor = 2.0
			# trait = wounded
		# }
		# modifier = {
			# factor = 2.0
			# trait = deceitful
		# }
		# modifier = {
			# factor = 2.0
			# trait = arbitrary
		# }
		# modifier = {
			# factor = 2.0
			# trait = cynical
		# }
		# modifier = {
			# factor = 2.0
			# trait = craven
		# }
		# modifier = {
			# factor = 2.0
			# trait = gardener
		# }
		# modifier = {
			# factor = 3.0
			# trait = infirm
		# }
		# modifier = {
			# factor = 3.0
			# trait = pneumonic
		# }
		# modifier = {
			# factor = 3.0
			# trait = syphilitic
		# }
		# modifier = {
			# factor = 3.0
			# trait = has_measles
		# }
		# modifier = {
			# factor = 3.0
			# trait = has_typhus
		# }
		# modifier = {
			# factor = 3.0
			# trait = has_tomb_rot
		# }
		# modifier = {
			# factor = 3.0
			# trait = has_creeping_buboes
		# }
		# modifier = {
			# factor = 3.0
			# trait = pneumonic
		# }
		# modifier = {
			# factor = 3.0
			# trait = has_bone_auge
		# }
		# modifier = {
			# factor = 5.0
			# trait = has_nurgles_rot
		# }
	# }
	# FROM = { character_event = { id = cultistplot.5 days = 7 } }
	# }
	
	# }
	
# character_event = {
# id = cultistplot.4
    # desc = EVTDESC_CULTISTPLOT_4 #"You sent [ROOT.plot_target_char.GetTitledName] an anonymous message to meet you alone in the sewers, but [ROOT.plot_target_char.GetSheHe] never showed up. Perhaps [ROOT.plot_target_char.GetSheHe] is not yet ready to embrace Nurgle. However, all come before the Maggot Lord eventually." 
    # picture = "GFX_evt_nurgle"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_4A #"All glory to Father Nurgle!"
	
		# increase_visibility_minor_effect = yes
	# }
	
	# }
	
# character_event = {
# id = cultistplot.5
    # desc = EVTDESC_CULTISTPLOT_5 #"You sent [ROOT.plot_target_char.GetTitledName] an anonymous message to meet you alone in the sewers, and [ROOT.plot_target_char.GetSheHe] stands before you. [ROOT.plot_target_char.GetSheHe] is covered in filth and looks half-mad, but they shall soon find solace in the arms of the Plaguefather." 
    # picture = "GFX_evt_nurgle"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = cultistplot.5.a # Come now, dear friend, Papa Nurgle will make things right...
	# change_variable = { which = nurglefavor value = 1 }
	# FROM = { character_event = { id = cultistplot.6  } } 
        # reverse_opinion = {
            # modifier = opinion_friend
            # who = FROM
            # years = 100
        # } 
	# }
	
	# }
	
# character_event = {
# id = cultistplot.6
    # desc = EVTDESC_CULTISTPLOT_6 #"You follow the directions to the letter and go alone into some sewer, deep underground. You soon find yourself surrounded by hooded figures in filthy, decaying robes. The plague ridden figures begin to chant words of power that you cannot resist..." 
    # picture = "GFX_evt_nurgle"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_6A #"Nurgle! Plaguefather! Lord of Decay! I shall serve you, just end my pain..."
	# set_character_flag = hidden_cultist_nurgle
        # reverse_opinion = {
            # modifier = opinion_friend
            # who = FROM
            # years = 100
        # } 
	# }
	
	# }
	
# ##Convert to Slaanesh Plot Chain##
# character_event = {
    # id = cultistplot.7
    # desc = EVTDESC_CULTISTPLOT_7 #"With the help of other acolytes and devotees of She who Thirsts, you have succeeded in placing several artefacts of Slaanesh in the chamber's of [ROOT.plot_target_char.GetTitledName]. [ROOT.plot_target_char.GetSheHeCap] should be experiencing the ecstasy of the Master of Excess even now. This pleasure will surely cause [ROOT.plot_target_char.GetTitledFirstName] to join us soon..."
    # picture = GFX_evt_whispers
    # border = GFX_event_normal_frame_intrigue
    
    # capable_only = yes
    # prisoner = no
    
    # trigger = {
    # has_plot = plot_convert_slaanesh
	# plot_target_char = {
            # prisoner = no
            # NOT = { trait = incapable }
        # }
	# plot_power_contribution = 
        # {
            # plotter = ROOT
            # power = 0.025
        # }
        # }
		
    # mean_time_to_happen = {
        # months = 360
        
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.10
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.20
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.30
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.40
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.50
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.60
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.70
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.80
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.90
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.5
            # }
        # }
        
        # modifier = {
            # factor = 0.33
            # any_backed_character = {
		# has_plot = plot_convert_slaanesh
                # plot_power = 1.5
        # }
		# }
        # modifier = {
            # factor = 0.33
            # any_backed_character = {
		# has_plot = plot_convert_slaanesh
                # plot_power = 1.75
        # }
		# }
        # modifier = {
            # factor = 0.33
            # any_backed_character = {
		# has_plot = plot_convert_slaanesh
                # plot_power = 2
        # }
		# }
    # }
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_7A #"Let us invite [ROOT.plot_target_char.GetTitledFirstName] to a party where we can show them the joys of the Prince of Pleasure."
        # trigger = {
            # plot_target_char = { NOT = { trait = hedonist } }
        # }
        # plot_target_char = { character_event = { id = cultistplot.8 days = 7 } }
    # }
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_7B #"The time has come to show [ROOT.plot_target_char.GetTitledFirstName] the sensation of the Lord of Delights."
        # trigger = {
            # plot_target_char = { trait = hedonist } 
        # }
        # plot_target_char = { character_event = { id = cultistplot.11 days = 7 } }
    # }
	
	# }
	
# character_event = {
# id = cultistplot.8
    # desc = EVTDESC_CULTISTPLOT_8 #"You have been invited to a feast, but you soon notice that this is no ordinary part. You see some of the other guests begin to overindulge in food and wine while others start performing lewd and licentious acts to themselves and others..." 
    # picture = "GFX_evt_feast"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_8A #"Time to leave!"
	 # ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = brave
      # }
      # modifier = {
         # factor = 4.0
         # trait = zealous
      # }
      # modifier = {
         # factor = 2.0
         # is_strong_trigger = yes
      # }
      # modifier = {
         # factor = 2.0
         # trait = just
      # }
      # modifier = {
         # factor = 2.0
         # trait = content
      # }
      # modifier = {
         # factor = 2.0
         # trait = diligent
      # }
      # modifier = {
         # factor = 2.0
         # trait = chaste
      # }
   # }
	# FROM = { character_event = { id = cultistplot.9 days = 7 }}
	# }
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_8B #"This is great, I'm staying!"
   # ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = craven
      # }
      # modifier = {
         # factor = 2.0
         # trait = arbitrary
      # }
      # modifier = {
         # factor = 2.0
         # trait = weak
      # }
      # modifier = {
         # factor = 2.0
         # trait = deceitful
      # }
      # modifier = {
         # factor = 2.0
         # trait = cynical
      # }
      # modifier = {
         # factor = 2.0
         # trait = lustful
      # }
      # modifier = {
         # factor = 2.0
         # trait = hedonist
      # }
      # modifier = {
         # factor = 2.0
         # trait = lunatic_1
      # }
      # modifier = {
         # factor = 3.0
         # trait = lunatic_2
      # }
      # modifier = {
         # factor = 4.0
         # trait = lunatic_3
      # }
   # }
# add_trait = hedonist
# add_trait = lustful
# FROM = { character_event = { id = cultistplot.10 days = 7 } }
# }

# }
	
# character_event = {
# id = cultistplot.9
    # desc = EVTDESC_CULTISTPLOT_9 #"You invited [ROOT.plot_target_char.GetTitledName] to a party where you made sure that the mood would turn to more carnal activities. However, it turns out that [ROOT.plot_target_char.GetSheHe] left early and didn't partake in the festivities."
    # picture = "GFX_evt_slaanesh"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = "EVTOPT_CULTISTPLOT_9A" #I'll keep trying."
	# }
	
	# }
	
# character_event = {
# id = cultistplot.10
    # desc = EVTDESC_CULTISTPLOT_10 #"You invited [ROOT.plot_target_char.GetTitledName] to a party where you made sure that the mood would turn to more carnal activities. Luckily [ROOT.plot_target_char.GetSheHe] enjoyed [ROOT.plot_target_char.GetHimselfHerself], and will soon follow the ways of the Lord of Dark Delights!" 
    # picture = "GFX_evt_slaanesh"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_10A #"The Master of Ecstasy will be pleased!"
	# }
	
	# }
	
# character_event = {
# id = cultistplot.11
    # desc = EVTDESC_CULTISTPLOT_11 #"Lately you have been haunted by unbearably pleasurable dreams where you engage in unspeakable acts with impossibly beautiful creatures with the features of both men and women. However, today you received an anonymous message: 'We can help you. Follow these directions and come alone...'"  
    # picture = "GFX_evt_slaanesh"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_11A #"No, never!"
	# ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = brave
      # }
      # modifier = {
         # factor = 4.0
         # trait = zealous
      # }
      # modifier = {
         # factor = 2.0
         # is_strong_trigger = yes
      # }
      # modifier = {
         # factor = 2.0
         # trait = just
      # }
      # modifier = {
         # factor = 2.0
         # trait = content
      # }
      # modifier = {
         # factor = 2.0
         # trait = diligent
      # }
   # }
   # custom_tooltip = { text = EVTTOOLTIP_CULTISTPLOT_11A } #"Going to a brothel alone at night will only make things worse.
	# add_trait = stressed
	# FROM = { character_event = { id = cultistplot.12 days = 7 } }
	# }
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_11B #"Maybe there's something to this message."
  # ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = craven
      # }
      # modifier = {
         # factor = 2.0
         # trait = arbitrary
      # }
      # modifier = {
         # factor = 2.0
         # trait = weak
      # }
      # modifier = {
         # factor = 2.0
         # trait = deceitful
      # }
      # modifier = {
         # factor = 2.0
         # trait = cynical
      # }
      # modifier = {
         # factor = 2.0
         # trait = lustful
      # }
      # modifier = {
         # factor = 2.0
         # trait = hedonist
      # }
      # modifier = {
         # factor = 2.0
         # trait = lunatic_1
      # }
      # modifier = {
         # factor = 3.0
         # trait = lunatic_2
      # }
      # modifier = {
         # factor = 4.0
         # trait = lunatic_3
      # }
   # }
   # custom_tooltip = { text = EVTTOOLTIP_CULTISTPLOT_11B } #"I could perhaps get some 'relief' at the brothel as well."
	# FROM = { character_event = { id = cultistplot.13 days = 7 } }
	# }
	
	# }
	
	
	
# character_event = {
# id = cultistplot.12
    # desc = EVTDESC_CULTISTPLOT_12 #"You sent [ROOT.plot_target_char.GetTitledName] an anonymous message to meet you alone in an isolated brothel, but [ROOT.plot_target_char.GetSheHe] never showed up. Perhaps [ROOT.plot_target_char.GetSheHe] is not yet ready to embrace Slaanesh. However, no one can resist the Prince of Pleasure forever." 
    # picture = "GFX_evt_slaanesh"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_12A #"Soon all will serve Slaanesh."
	# change_variable = { which = slaaneshfavor value = 1 }
	# increase_visibility_minor_effect = yes
	# }
	
	# }
	
# character_event = {
# id = cultistplot.13
    # desc = EVTDESC_CULTISTPLOT_13 #"You sent [ROOT.plot_target_char.GetTitledName] an anonymous message to meet you alone in an isolated brothel, and now [ROOT.plot_target_char.GetSheHe] stands before you. You can see [ROOT.plot_target_char.GetHerHis] desire for more sensation in [ROOT.plot_target_char.GetHerHis] eyes as you begin to instruct [ROOT.plot_target_char.GetHerHim] in the ways of the Lord of Dark Delights." 
    # picture = "GFX_evt_slaanesh"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_13A #"We welcome you!"
	# change_variable = { which = slaaneshfavor value = 1 }
	# FROM = { character_event = { id = cultistplot.14  } } 
        # reverse_opinion = {
            # modifier = opinion_friend
            # who = FROM
            # years = 100
        # } 
	# }
	
	# }
	
# character_event = {
# id = cultistplot.14
    # desc = EVTDESC_CULTISTPLOT_14 #"You follow the instructions to the letter and go alone to a remote brothel. There you see people engaging in all manner of unspeakable acts. As they moan and cry out dark words power, you find yourself unable to resist the temptation the words bring..." 
    # picture = "GFX_evt_slaanesh"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_14A #"Slaanesh! Prince of Chaos! Lord of Excess! I shall follow you into a world of endless sensations."
	# set_character_flag = hidden_cultist_slaanesh
        # reverse_opinion = {
            # modifier = opinion_friend
            # who = FROM
            # years = 100
        # } 
	# }
	
	# }
	
	# ##Convert to Tzeentch Plot Chain##
# character_event = {
    # id = cultistplot.15
    # desc = EVTDESC_CULTISTPLOT_15 #"With the help of other acolytes and followers of the Changer of Ways, you have succeeded in placing several artefacts of Tzeentch in the chamber's of [ROOT.plot_target_char.GetTitledName]. [ROOT.plot_target_char.GetSheHeCap] should be filled with the ambition of the Architect of Fate even now. [ROOT.plot_target_char.GetTitledFirstName] cannot escape change and will join us soon..."
    # picture = GFX_evt_whispers
    # border = GFX_event_normal_frame_intrigue
    
    # capable_only = yes
    # prisoner = no
    
    # trigger = {
    # has_plot = plot_convert_tzeentch
	# plot_target_char = {
            # prisoner = no
            # NOT = { trait = incapable }
        # }
	# plot_power_contribution = 
        # {
            # plotter = ROOT
            # power = 0.025
        # }
        # }
		
    # mean_time_to_happen = {
        # months = 360
        
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.10
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.20
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.30
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.40
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.50
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.60
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.70
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.80
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.90
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.5
            # }
        # }
        
        # modifier = {
            # factor = 0.33
                # plot_power = 1.0
		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 1.5
		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 2.0
		# }
    # }
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_15A #"Let us send [ROOT.plot_target_char.GetTitledFirstName] this book describing the true state of things."
        # trigger = {
            # plot_target_char = { NOT = { trait = mystic } }
        # }
        # plot_target_char = { character_event = { id = cultistplot.16 days = 7 } }
    # }
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_15B #"The time has come to show [ROOT.plot_target_char.GetTitledFirstName] the splendour of the Master of Deceit."
        # trigger = {
            # plot_power = 1.0
			# OR = { plot_target_char = { trait = mystic } }
        # }
        # plot_target_char = { character_event = { id = cultistplot.19 days = 7 } }
    # }
	
	# }
	
# character_event = {
# id = cultistplot.16
    # desc = EVTDESC_CULTISTPLOT_16 #"A book without a title was pushed into my hands by a hooded man clad in midnight blue. Something was said, but was lost in the confusion. The book was still there." 
    # picture = "GFX_evt_library"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_16A #"Hey... wait!"
	 # ai_chance = {
      # factor = 25
      # modifier = {
         # factor = 2.0
         # trait = ambitious
      # }
      # modifier = {
         # factor = 2.0
         # trait = arbitrary
      # }
      # modifier = {
         # factor = 2.0
         # trait = deceitful
      # }
      # modifier = {
         # factor = 2.0
         # trait = cynical
      # }
      # modifier = {
         # factor = 4.0
         # trait = mystic
      # }      modifier = {
         # factor = 2.0
         # trait = lunatic_1
      # }
      # modifier = {
         # factor = 3.0
         # trait = lunatic_2
      # }
      # modifier = {
         # factor = 4.0
         # trait = lunatic_3
      # }

   # }
   # add_trait = mystic
	# FROM = { character_event = { id = cultistplot.18 days = 7 }}
	# }
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_16B #"It wants me to read it...."
	 # ai_chance = {
      # factor = 25
      # modifier = {
         # factor = 2.0
         # trait = ambitious
      # }
      # modifier = {
         # factor = 2.0
         # trait = arbitrary
      # }
      # modifier = {
         # factor = 2.0
         # trait = deceitful
      # }
      # modifier = {
         # factor = 2.0
         # trait = cynical
      # }
      # modifier = {
         # factor = 4.0
         # trait = mystic
      # }
      # modifier = {
         # factor = 2.0
         # trait = lunatic_1
      # }
      # modifier = {
         # factor = 3.0
         # trait = lunatic_2
      # }
      # modifier = {
         # factor = 4.0
         # trait = lunatic_3
      # }
   # }
   # add_trait = mystic
	# FROM = { character_event = { id = cultistplot.18 days = 7 }}
	# }
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_16C #"Burn it! It is clearly a heretical text."
      # ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = brave
      # }
      # modifier = {
         # factor = 4.0
         # trait = zealous
      # }
      # modifier = {
         # factor = 2.0
         # is_strong_trigger = yes
      # }
      # modifier = {
         # factor = 2.0
         # trait = just
      # }
      # modifier = {
         # factor = 2.0
         # trait = content
      # }
      # modifier = {
         # factor = 2.0
         # trait = diligent
      # }
   # }
# FROM = { character_event = { id = cultistplot.17 days = 7 } }
# }

# }
	
# character_event = {
# id = cultistplot.17
    # desc = EVTDESC_CULTISTPLOT_17 #"[ROOT.plot_target_char.GetTitledName] has the book in his possession, and your agents inform you that [ROOT.plot_target_char.GetSheHe] has spent a great deal of time lately trying to decipher its secrets."
    # picture = "GFX_evt_tzeentch"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_17A #"Excellent! Soon, he will be ready to accept the ways of the Great Mutator."
	# change_variable = { which = tzeentchfavor value = 1 }
	# }
	
	# }
	
# character_event = {
# id = cultistplot.18
    # desc = EVTDESC_CULTISTPLOT_18 #"[ROOT.plot_target_char.GetTitledName] had the book in his possession, but your agents inform you that [ROOT.plot_target_char.GetSheHe] has burned it without opening it!"  
    # picture = "GFX_evt_tzeentch"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_18A #"Only a fool would try escape the schemes of the Great Conspirator."
	# increase_visibility_minor_effect = yes
	# }
	
	# }
	
# character_event = {
# id = cultistplot.19
    # desc = EVTDESC_CULTISTPLOT_19 #"Lately you have been haunted by maniacal dreams filled with riotous colours and creature beyond imagining, while your waking moments are spent plotting to acquire even more than what you already have. However, today you received an anonymous message: 'We can help you achieve your goals. Follow these directions and come alone...'" 
    # picture = "GFX_evt_tzeentch"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_19A #"No, never! Nothing comes for free, there is always catch..."
 # ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = brave
      # }
      # modifier = {
         # factor = 4.0
         # trait = zealous
      # }
      # modifier = {
         # factor = 2.0
         # is_strong_trigger = yes
      # }
      # modifier = {
         # factor = 2.0
         # trait = just
      # }
      # modifier = {
         # factor = 2.0
         # trait = content
      # }
      # modifier = {
         # factor = 2.0
         # trait = diligent
      # }
   # }
	# add_trait = stressed
	# FROM = { character_event = { id = cultistplot.20 days = 7 } }
	# }
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_19B #"If this person at the inn can help me, I would be stupid not to go."
 	 # ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = ambitious
      # }
      # modifier = {
         # factor = 2.0
         # trait = arbitrary
      # }
      # modifier = {
         # factor = 2.0
         # trait = deceitful
      # }
      # modifier = {
         # factor = 2.0
         # trait = cynical
      # }
      # modifier = {
         # factor = 4.0
         # trait = mystic
      # }
      # modifier = {
         # factor = 2.0
         # trait = lunatic_1
      # }
      # modifier = {
         # factor = 3.0
         # trait = lunatic_2
      # }
      # modifier = {
         # factor = 4.0
         # trait = lunatic_3
      # }
   # }
	# FROM = { character_event = { id = cultistplot.21 days = 7 } }
	# }
	
	# }
	
	
	
# character_event = {
# id = cultistplot.20
    # desc = EVTDESC_CULTISTPLOT_20 #"You sent [ROOT.plot_target_char.GetTitledName] an anonymous message to meet you alone in an isolated inn, but [ROOT.plot_target_char.GetSheHe] never showed up. Perhaps [ROOT.plot_target_char.GetSheHe] is not yet ready to embrace Tzeentch. However, none can escape the Changer of Ways." 
    # picture = "GFX_evt_tzeentch"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_20A #"Soon all will serve Tzeentch!"
	# change_variable = { which = tzeentchfavor value = 1 }
	# increase_visibility_minor_effect = yes
	# }
	
	# }
	
# character_event = {
# id = cultistplot.21
    # desc = EVTDESC_CULTISTPLOT_21 #"You sent [ROOT.plot_target_char.GetTitledName] an anonymous message to meet you alone in an isolated inn, and [ROOT.plot_target_char.GetSheHe] stands before you. You speak to him of Tzeentch and the power that awaits, and soon enough, [ROOT.plot_target_char.GetSheHe] gives in..." 
    # picture = "GFX_evt_tzeentch"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_21A #"We welcome you!"
	# change_variable = { which = tzeentchfavor value = 1 }
	# FROM = { character_event = { id = cultistplot.22  } } 
        # reverse_opinion = {
            # modifier = opinion_friend
            # who = FROM
            # years = 100
        # } 
	# }
	
	# }
	
# character_event = {
# id = cultistplot.22
    # desc = EVTDESC_CULTISTPLOT_22 #"You follow the instructions to the letter and go alone to a remote inn. There you find a group of hooded people, each wearing a different sinister mask. They begin whispering fell words of power and you know you need only take their potency for your own..."
    # picture = "GFX_evt_tzeentch"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_22A #"Tzeentch! Father of Lies! the Great Mutator! I shall devote myself to your ever-changing ways."
	# set_character_flag = hidden_cultist_tzeentch
        # reverse_opinion = {
            # modifier = opinion_friend
            # who = FROM
            # years = 100
        # } 
	# }
	
	# }
	
# ##Convert to Khorne Plot Chain##
# character_event = {
    # id = cultistplot.23
    # desc = EVTDESC_CULTISTPLOT_23 #"With the help of other acolytes and fanatics of the Blood God, you have succeeded in placing several artefacts of Khorne in the chamber's of [ROOT.plot_target_char.GetTitledName]. [ROOT.plot_target_char.GetSheHeCap] should be filled with the rage Brass Lord even now. This blood-lust will no doubt cause [ROOT.plot_target_char.GetTitledFirstName] to join us soon..."
    # picture = GFX_evt_whispers
    # border = GFX_event_normal_frame_intrigue
    
    # capable_only = yes
    # prisoner = no
    
    # trigger = {
    # has_plot = plot_convert_khorne
	# plot_target_char = {
            # prisoner = no
            # NOT = { trait = incapable }
        # }
	# plot_power_contribution = 
        # {
            # plotter = ROOT
            # power = 0.025
        # }
        # }
		
    # mean_time_to_happen = {
        # months = 360
        
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.10
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.20
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.30
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.40
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.50
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.60
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.70
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.80
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.90
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.5
            # }
        # }
        
        # modifier = {
            # factor = 0.33
                # plot_power = 1.0
		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 1.5

		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 2.0

		# }
    # }
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_23A #"Let us send [ROOT.plot_target_char.GetTitledFirstName] this weapon dedicated to the Lord of the Skull Throne."
        # trigger = {
            # plot_target_char = { NOT = { trait = cruel } }
        # }
        # plot_target_char = { character_event = { id = cultistplot.24 days = 7 } }
    # }
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_23B #"The time has come to show [ROOT.plot_target_char.GetTitledFirstName] the glory of Khorne."
        # trigger = {
            # plot_power = 1.0
			# OR = { plot_target_char = { trait = cruel } }
        # }
        # plot_target_char = { character_event = { id = cultistplot.27 days = 7 } }
    # }
	
	# }
	
# character_event = {
# id = cultistplot.24
    # desc = EVTDESC_CULTISTPLOT_24 #"A package arrived for you today that contained a masterfully crafted weapon. However, no matter how many times you clean it, you can't seem to remove the bloodstains the blade..." 
    # picture = "GFX_evt_whispers"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_24A #"Bloodstains never injured anyone, it's the injury that makes the bloodstain. Ha!"
	 # ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = ambitious
      # }
      # modifier = {
         # factor = 2.0
         # trait = arbitrary
      # }
      # modifier = {
         # factor = 2.0
         # trait = deceitful
      # }
      # modifier = {
         # factor = 2.0
         # trait = cynical
      # }
      # modifier = {
         # factor = 4.0
         # trait = mystic
      # }
      # modifier = {
         # factor = 2.0
         # trait = lunatic_1
      # }
      # modifier = {
         # factor = 3.0
         # trait = lunatic_2
      # }
      # modifier = {
         # factor = 4.0
         # trait = lunatic_3
      # }
   # }
   # add_trait = cruel
   # change_martial = 2
	# FROM = { character_event = { id = cultistplot.25 days = 7 }}
	# }
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_24B #"This is an ill omen. I don't care how fine a weapon it is, destroy it!"
      # ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = brave
      # }
      # modifier = {
         # factor = 4.0
         # trait = zealous
      # }
      # modifier = {
         # factor = 2.0
         # is_strong_trigger = yes
      # }
      # modifier = {
         # factor = 2.0
         # trait = just
      # }
      # modifier = {
         # factor = 2.0
         # trait = content
      # }
      # modifier = {
         # factor = 2.0
         # trait = diligent
      # }
   # }
# FROM = { character_event = { id = cultistplot.26 days = 7 } }
# }

# }
	
# character_event = {
# id = cultistplot.25
    # desc = EVTDESC_CULTISTPLOT_25 #"You delivered the weapon to [ROOT.plot_target_char.GetTitledName], and your agents inform you that [ROOT.plot_target_char.GetSheHe] has grown quite fond of it, using it upon any who displease [ROOT.plot_target_char.GetHerHim]."  
    # picture = "GFX_evt_khorne"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_25A #khornecultist.10.a # Blood for the Blood God! SKulls for the Skull Throne!
	# change_variable = { which = khornefavor value = 1 }
	# }
	
	# }
	
# character_event = {
# id = cultistplot.26
    # desc = EVTDESC_CULTISTPLOT_26 #"You delivered the weapon to [ROOT.plot_target_char.GetTitledName], however your agents inform you that [ROOT.plot_target_char.GetSheHe] destroyed soon after. HOW DARE HE DENY KHORNE!" 
    # picture = "GFX_evt_khorne"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_26 #khornecultist.10.a # Blood for the Blood God! SKulls for the Skull Throne!
	# increase_visibility_minor_effect = yes
	# }
	
	# }
	
# character_event = {
# id = cultistplot.27
    # desc = EVTDESC_CULTISTPLOT_27 #"Lately you have been haunted by dreams of endless slaughter. In them you crush your enemies and see them driven before you. You awake as you hear the lamentations of their women. However, today you received an anonymous message: 'We can help you. Follow these directions and come alone...'"
    # picture = "GFX_evt_khorne"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_27A #"No, never!"
  # ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = brave
      # }
      # modifier = {
         # factor = 4.0
         # trait = zealous
      # }
      # modifier = {
         # factor = 2.0
         # trait = just
      # }
      # modifier = {
         # factor = 2.0
         # trait = content
      # }
      # modifier = {
         # factor = 2.0
         # trait = diligent
      # }
   # }
   # custom_tooltip = { text = EVTTOOLTIP_CULTISTPLOT_27A } #"What good could come of going to fighting pit alone at night?"
	# add_trait = stressed
	# FROM = { character_event = { id = cultistplot.28 days = 7 } }
	# }
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_27B #"Sounds Fun!"
 	   # ai_chance = {
      # factor = 50
      # modifier = {
         # factor = 2.0
         # trait = ambitious
      # }
      # modifier = {
         # factor = 2.0
         # is_strong_trigger = yes
      # }
      # modifier = {
         # factor = 4.0
         # trait = cruel
      # }
      # modifier = {
         # factor = 2.0
         # trait = impaler
      # }
   # }
   # custom_tooltip = { text = EVTTOOLTIP_CULTISTPLOT_27B } #"Maybe I can even bash some heads too..."
	# FROM = { character_event = { id = cultistplot.29 days = 7 } }
	# }
	
	# }
	
	
	
# character_event = {
# id = cultistplot.28
    # desc = EVTDESC_CULTISTPLOT_28 #"You sent [ROOT.plot_target_char.GetTitledName] an anonymous message to meet you alone in an isolated fighting pit, but [ROOT.plot_target_char.GetSheHe] never showed up. Perhaps [he/she] is not yet ready to shed blood for Khorne. However, all will bleed for the Blood God before long." 
    # picture = "GFX_evt_khorne"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_28A #khornecultist.10.a # Blood for the Blood God! SKulls for the Skull Throne!
	# change_variable = { which = khornefavor value = 1 }
	# increase_visibility_minor_effect = yes
	# }
	
	# }
	
# character_event = {
# id = cultistplot.29
    # desc = EVTDESC_CULTISTPLOT_29 #"You sent [ROOT.plot_target_char.GetTitledName] an anonymous message to meet you alone in an isolated fighting pit, and [ROOT.plot_target_char.GetSheHe] stands before you. You see the bloodlust in [ROOT.plot_target_char.GetHerHis] eyes and tell [ROOT.plot_target_char.GetHerHim] to take [ROOT.plot_target_char.GetHerHis] fill..."  
    # picture = "GFX_evt_khorne"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_29A #khornecultist.10.a # Blood for the Blood God! SKulls for the Skull Throne!
	# change_variable = { which = khornefavor value = 1 }
	# FROM = { character_event = { id = cultistplot.30  } } 
        # reverse_opinion = {
            # modifier = opinion_friend
            # who = FROM
            # years = 100
        # } 
	# }
	
	# }
	
# character_event = {
# id = cultistplot.30
    # desc = EVTDESC_CULTISTPLOT_30 #"You follow the instructions to the letter and go alone to a remote fighting pit. There a person cloaked in red tells you to take your fill killing the captives you see around you. Their screams are the sweetest sound you ever heard..." 
    # picture = "GFX_evt_khorne"
    # min_age = 16
    # capable_only = yes
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_30A #khornecultist.10.a # Blood for the Blood God! SKulls for the Skull Throne!
	# set_character_flag = hidden_cultist_khorne
        # reverse_opinion = {
            # modifier = opinion_friend
            # who = FROM
            # years = 100
        # } 
	# }
	
	# }
	
# #########Now for the plots to damage a province#########

# character_event = {
    # id = cultistplot.31
    # desc = EVTDESC_CULTISTPLOT_31 #"With the help of various backers - both witting and unwitting - you are now in a position to bring the gifts of the Plaguefather to [ROOT.plot_target_char.GetTitledName]'s homelands. Grandfather Nurgle will be pleased!"
    # picture = GFX_evt_nurgle
    # border = GFX_event_normal_frame_intrigue
    
    # capable_only = yes
    # prisoner = no
    
    # trigger = {
    # has_plot = plot_province_disease
	# plot_target_char = {
            # prisoner = no
            # NOT = { trait = incapable }
        # }
	# plot_power_contribution = 
        # {
            # plotter = ROOT
            # power = 0.025
        # }
        # }
		
    # mean_time_to_happen = {
        # months = 360
        
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.10
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.20
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.30
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.40
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.50
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.60
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.70
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.80
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.90
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.5
            # }
        # }
        
        # modifier = {
            # factor = 0.33
                # plot_power = 1.0

		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 1.5
		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 2.0

		# }
    # }
	
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_31A #"All should share in the gifts of the Plague Lord""
        # plot_target_char = { character_event = { id = cultistplot.32 days = 7 } }
		# change_variable = { which = nurglefavor value = 1 }
		# increase_visibility_minor_effect = yes
    # }
	
	# }

# #target is informed#
# character_event = {
    # id = cultistplot.32
    # desc = EVTDESC_CULTISTPLOT_32 #"An outbreak of a particularly virulent strain of disease has occurred in one of your provinces. Evidence suggests this is the work of cultists devoted to the Ruinous Powers! All you can do now is try to contain the outbreak and redouble your efforts to root out cultists in your midst!"
    # picture = GFX_evt_nurgle
    # border = GFX_event_normal_frame_intrigue
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_32A #"[ROOT.GetRandomGodNameCap] save us from these plagues!" 
		# location = { 
			# add_province_modifier = { name = nurgle_province_disease duration = 1500 } 
		# }
		# random_list = {
				# 16 = {
					# modifier = {
					  # factor = 5
					  # trait = has_tuberculosis
					# }
					# location = {
						# spawn_disease = tuberculosis 
					# }
				# }
				# 16 = {
					# modifier = {
					  # factor = 5
					  # trait = has_typhoid_fever
					# }
					# location = {
						# spawn_disease = typhoid_fever 
					# }
				# }
				# 16 = {
					# modifier = {
					  # factor = 5
					  # trait = has_typhus
					# }
					# location = {
						# spawn_disease = typhus 
					# }
				# }
				# 16 = {
					# modifier = {
					  # factor = 5
					  # trait = has_measles
					# }
					# location = {
						# spawn_disease = measles 
					# }
				# }
				# 16 = {
					# modifier = {
					  # factor = 5
					  # trait = has_small_pox
					# }
					# location = {
						# spawn_disease = small_pox 
					# }
				# }
				# 16 = {
					# modifier = {
					  # factor = 5
					  # trait = has_aztec_disease
					# }
					# location = {
						# spawn_disease = syphilis 
					# }
				# }
		# }
	# }
	
	# }
	
# character_event = {
    # id = cultistplot.33
    # desc = EVTDESC_CULTISTPLOT_33 #"With the help of various backers - both witting and unwitting - you are now in a position to incite a tribe of Beastmen to ravage [ROOT.plot_target_char.GetTitledName]'s homelands. Khorne will be pleased!"
    # picture = GFX_evt_khorne
    # border = GFX_event_normal_frame_intrigue
    
    # capable_only = yes
    # prisoner = no
    
    # trigger = {
    # has_plot = plot_beastmen_unrest
	# plot_target_char = {
            # prisoner = no
            # NOT = { trait = incapable }
        # }
	# plot_power_contribution = 
        # {
            # plotter = ROOT
            # power = 0.025
        # }
        # }
		
    # mean_time_to_happen = {
        # months = 360
        
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.10
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.20
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.30
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.40
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.50
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.60
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.70
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.80
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.90
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.5
            # }
        # }
        
        # modifier = {
            # factor = 0.33
                # plot_power = 1.0

		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 1.5

		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 2.0
		# }
    # }
	
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_33A #khornecultist.10.a # Blood for the Blood God! SKulls for the Skull Throne!
        # plot_target_char = { character_event = { id = cultistplot.34 days = 7 } }
		# change_variable = { which = khornefavor value = 1 }
		# increase_visibility_minor_effect = yes
    # }
	
	# }

# #target is informed#
# character_event = {
    # id = cultistplot.34
    # desc = EVTDESC_CULTISTPLOT_34 #"Your marshal informs you that a tribe of Beastmen has started attacking one of your provinces, raiding villages and burning homesteads. Evidence suggests this is the work of cultists devoted to the Ruinous Powers! However your people are blaming you for the lack of protection."
    # picture = GFX_evt_khorne
    # border = GFX_event_normal_frame_intrigue
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_34A #"From the fury of the Beastmen, good [ROOT.GetRandomGodNameCap], deliver us!"
	# location = { 
	# add_province_modifier = { name = beastmen_unrest duration = 1500 }  
	# }
	# }
	
	# }
	
# character_event = {
    # id = cultistplot.35
    # desc = EVTDESC_CULTISTPLOT_35 #"With the help of various backers - both witting and unwitting - you are now in a position to poison [ROOT.plot_target_char.GetTitledName]'s provinces with warpdust, which will lead to an increase in mutations. Tzeentch will be pleased!"
    # picture = GFX_evt_tzeentch
    # border = GFX_event_normal_frame_intrigue
    
    # capable_only = yes
    # prisoner = no
    
    # trigger = {
    # has_plot = plot_warpdust
	# plot_target_char = {
            # prisoner = no
            # NOT = { trait = incapable }
        # }
	# plot_power_contribution = 
        # {
            # plotter = ROOT
            # power = 0.5
        # }
        # }
		
    # mean_time_to_happen = {
        # months = 360
        
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.65
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.75
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.85
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.90
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.25
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.50
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.70
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.80
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.90
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 2
            # }
        # }
        
        # modifier = {
            # factor = 0.33
                # plot_power = 1.25
  
		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 1.5

		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 2.0
		# }
    # }
	
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_35A #"Glory to the Great Mutator!"
        # plot_target_char = { character_event = { id = cultistplot.36 days = 7 } }
		# change_variable = { which = tzeentchfavor value = 1 }
		# increase_visibility_minor_effect = yes
    # }
	
	# }

# #target is informed#
# character_event = {
    # id = cultistplot.36
    # desc = EVTDESC_CULTISTPLOT_36 #"Your steward tells you that traces of warpdust have been found in one of your province. Evidence suggests this is the work of cultists devoted to the Ruinous Powers! We will likely see an increased number of mutations, perhaps even in your own court, until the warpdust dissipates!"
    # picture = GFX_evt_tzeentch
    # border = GFX_event_normal_frame_intrigue
	
	# is_triggered_only = yes
	
	# option = {
		# name = EVTOPT_CULTISTPLOT_36A #"[ROOT.GetRandomGodNameCap] protect us from the gaze of Morrslieb!"
		# location = { 
		# add_province_modifier = { name = warpdust_traces duration = 1500 }  
		# }
	# }
	
# }
	
# character_event = {
    # id = cultistplot.37
    # desc = EVTDESC_CULTISTPLOT_37 #"With the help of various backers - both witting and unwitting - you are now in a position to fill [ROOT.plot_target_char.GetTitledName]'s province with drugs, wine, and orgies. Slaanesh will be pleased!"
    # picture = GFX_evt_slaanesh
    # border = GFX_event_normal_frame_intrigue
    
    # capable_only = yes
    # prisoner = no
    
    # trigger = {
    # has_plot = plot_hedonism
	# plot_target_char = {
            # prisoner = no
            # NOT = { trait = incapable }
        # }
	# plot_power_contribution = 
        # {
            # plotter = ROOT
            # power = 0.025
        # }
        # }
		
    # mean_time_to_happen = {
        # months = 360
        
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.10
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.20
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.30
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.40
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.50
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.60
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.70
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.80
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 0.90
            # }
        # }
        # modifier = {
            # factor = 0.7
            # plot_power_contribution = 
            # {
                # plotter = ROOT
                # power = 1.5
            # }
        # }
        
        # modifier = {
            # factor = 0.33
                # plot_power = 1.0
		# }
        # modifier = {
            # factor = 0.33
            # plot_power = 1.5

		# }
        # modifier = {
            # factor = 0.33
                # plot_power = 2.0
        # }
		# }
	
	
	 # option = {
        # name = EVTOPT_CULTISTPLOT_37A #"Glory to the Prince of Pleasure!"
        # plot_target_char = { character_event = { id = cultistplot.38 days = 7 } }
		# change_variable = { which = slaaneshfavor value = 1 }
		# increase_visibility_minor_effect = yes
    # }
	
	# }

# #target is informed#
# character_event = {
    # id = cultistplot.38
    # desc = EVTDESC_CULTISTPLOT_38 #"Your steward informs you that your province has been unruly and downright un-pious. The peasants seem to drink, whore,and gamble away all their income, and even some of the better bred citizens do the same."
    # picture = GFX_evt_slaanesh
    # border = GFX_event_normal_frame_intrigue
	
	# is_triggered_only = yes
	
	# option = {
	# name = EVTOPT_CULTISTPLOT_38A #"But... they'll still pay taxes, right?"
	# location = { 
	# add_province_modifier = { name = slaanesh_hedonism duration = 1500 }  
	# }
	# }
	
# }